# Руководство по работе с несколькими историями

## Обзор

Система теперь поддерживает работу с несколькими историями, каждая из которых хранится в отдельном bucket в Supabase. Это позволяет:

- Создавать и управлять несколькими независимыми историями
- Хранить ресурсы каждой истории отдельно
- Быстро переключаться между историями

## Архитектура

### Структура хранения

Каждая история = отдельный bucket в Supabase Storage:

```
Supabase Storage
├── my_dracula_story/          # Bucket 1
│   ├── dracula_story.json  # Файл истории
│   ├── background1.jpg
│   ├── character1.png
│   └── animation.riv
├── zombie_apocalypse/         # Bucket 2
│   ├── story.json
│   ├── zombie_bg.jpg
│   └── ...
└── fairy_tale/                # Bucket 3
    ├── tale.json
    └── ...
```

### Изменения в коде

#### 1. Удален жестко заданный `bucketName`
**Файл:** `src/lib/supabaseClient.ts`

```typescript
// Было:
export const bucketName = 'dracula';

// Стало:
// bucketName больше не нужен, используется editor.selectedBucket
```

#### 2. Добавлено состояние для работы с bucket'ами
**Файл:** `src/lib/editor/stores/editorStore.svelte.ts`

```typescript
interface EditorState {
  // ... другие поля
  
  // Выбранная история (bucket)
  selectedBucket: string | null;
  availableBuckets: BucketInfo[];
}
```

#### 3. Создан новый store для работы с bucket'ами
**Файл:** `src/lib/editor/stores/bucketStore.ts`

Функции:
- `fetchBucketsList()` — получить список всех bucket'ов
- `createStoryBucket(bucketName)` — создать новый bucket
- `deleteStoryBucket(bucketName)` — удалить bucket
- `getCurrentBucket()` — получить текущий выбранный bucket
- `isBucketSelected()` — проверить, выбран ли bucket

#### 4. Обновлены все функции работы с ресурсами
**Файлы:**
- `src/lib/editor/stores/resourceStore.ts`
- `src/lib/editor/stores/storyStore.ts`

Теперь все функции используют `editor.selectedBucket` вместо фиксированного `bucketName`:

```typescript
// Было:
await supabase.storage.from(bucketName).download(fileName);

// Стало:
await supabase.storage.from(editor.selectedBucket).download(fileName);
```

#### 5. Создан компонент выбора истории
**Файл:** `src/lib/editor/components/StorySelector.svelte`

UI компонент для:
- Отображения списка доступных историй
- Создания новой истории
- Удаления истории
- Выбора истории для редактирования

## Использование

### В редакторе

1. **При запуске редактора**:
   - Отображается экран выбора истории (`StorySelector`)
   - Загружается список всех bucket'ов из Supabase

2. **Создание новой истории**:
   - Нажмите кнопку "+ Создать историю"
   - Введите название (латинские буквы, цифры, нижние подчеркивания)
   - Нажмите "Создать"
   - Автоматически создастся новый bucket и откроется редактор

3. **Выбор существующей истории**:
   - Нажмите кнопку "Выбрать" на карточке истории
   - Загрузятся все файлы и ресурсы этой истории

4. **Удаление истории**:
   - Нажмите на иконку 🗑️ на карточке истории
   - Подтвердите удаление
   - **Внимание:** Это действие необратимо!

5. **Смена истории**:
   - Нажмите кнопку "← Сменить историю" в верхней панели редактора
   - Вернетесь к экрану выбора

### Программное использование

#### Получить текущий bucket:
```typescript
import { getCurrentBucket } from './stores/bucketStore';

const currentBucket = getCurrentBucket();
console.log('Текущий bucket:', currentBucket);
```

#### Загрузить список bucket'ов:
```typescript
import { fetchBucketsList } from './stores/bucketStore';

await fetchBucketsList();
```

#### Создать новую историю:
```typescript
import { createStoryBucket } from './stores/bucketStore';

const success = await createStoryBucket('my_new_story');
if (success) {
  console.log('История создана!');
}
```

#### Выбрать историю:
```typescript
import { editorActions } from './stores/editorStore.svelte';

editorActions.selectBucket('my_story');
```

## Важные замечания

### Права доступа Supabase

Для работы с bucket'ами необходимы соответствующие права в Supabase:

```sql
-- Создание bucket'ов (только для admin)
-- Обычно выполняется вручную в Supabase Dashboard

-- Чтение списка bucket'ов
-- Требует прав admin или service_role

-- Работа внутри bucket'ов (загрузка/удаление файлов)
-- Обычно достаточно прав пользователя
```

**Внимание:** Если у пользователя нет прав на чтение списка bucket'ов, 
функция `fetchBucketsList()` вернет пустой массив.

### Альтернатива: список разрешенных bucket'ов

Если нет прав admin, можно использовать список разрешенных bucket'ов:

```typescript
// В editorStore.svelte.ts
const ALLOWED_BUCKETS = ['dracula', 'zombie', 'fairy_tale'];

export const editor = $state<EditorState>({
  // ...
  availableBuckets: ALLOWED_BUCKETS.map(name => ({
    id: name,
    name,
    public: false,
    file_count: 0,
    created_at: new Date().toISOString()
  })),
});
```

### Безопасность

- Каждая история изолирована в своем bucket
- Нельзя получить доступ к файлам другой истории
- Удаление bucket удаляет все файлы без возможности восстановления

## Требования к Supabase

Для полноценной работы с несколькими историями:

1. **Права на управление bucket'ами** (для создания/удаления):
   - Роль: `service_role` или `admin`
   - Или использовать список разрешенных bucket'ов

2. **Права на работу с файлами**:
   - `SELECT` на таблицу `storage.objects`
   - `INSERT` для загрузки файлов
   - `DELETE` для удаления файлов

3. **RLS политики** (Row Level Security):
   ```sql
   -- Пример политики для доступа к файлам
   CREATE POLICY "Users can access their own story buckets"
   ON storage.objects
   FOR SELECT
   USING (bucket_id IN (
     -- Список bucket'ов, к которым есть доступ
   ));
   ```

## Миграция существующих историй

Если у вас уже есть история в bucket'е `dracula`, она будет автоматически 
доступна в новом интерфейсе. Дополнительные действия не требуются.

Для создания новых историй просто используйте интерфейс выбора историй
в редакторе.

## Тестирование

### Локально с Supabase

1. Убедитесь, что Supabase проект настроен и доступен
2. Проверьте переменные окружения:
   ```env
   VITE_SUPABASE_URL=your_supabase_url
   VITE_SUPABASE_ANON_KEY=your_anon_key
   ```
3. Запустите редактор: `npm run dev`
4. Откройте страницу редактора
5. Убедитесь, что отображается экран выбора историй

### Создание тестовой истории

1. Нажмите "+ Создать историю"
2. Введите: `test_story`
3. Нажмите "Создать"
4. Убедитесь, что открылся редактор
5. Создайте диалог и сохраните
6. Проверьте в Supabase Dashboard, что bucket `test_story` создан

## Troubleshooting

### Проблема: Список историй пуст

**Возможные причины:**
1. Нет прав на чтение списка bucket'ов
2. Нет bucket'ов в проекте

**Решение:**
- Проверьте права пользователя в Supabase
- Создайте bucket вручную в Supabase Dashboard
- Или используйте список разрешенных bucket'ов

### Проблема: Ошибка при создании истории

**Возможные причины:**
1. Некорректное название (спецсимволы, кириллица)
2. Bucket с таким именем уже существует
3. Нет прав на создание bucket'ов

**Решение:**
- Используйте только латинские буквы, цифры и `_`
- Проверьте список существующих bucket'ов
- Проверьте права пользователя

### Проблема: Не загружаются файлы

**Возможные причины:**
1. Bucket не выбран
2. Нет прав на чтение файлов в bucket

**Решение:**
- Убедитесь, что выбрана история
- Проверьте RLS политики для bucket'а

## Дополнительные ресурсы

- [Supabase Storage Documentation](https://supabase.com/docs/guides/storage)
- [Svelte 5 Runes Documentation](https://svelte.dev/docs/runes)
- [Основная документация проекта](../KODA.md)

---

**Обновлено:** 2025
**Версия:** 1.0
